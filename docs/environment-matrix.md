# Environment-variable matrix

Values shown here are names or modes, never credentials. Store local values in ignored `.env` files, CI test values in ephemeral job environment variables, and staging/production values in the relevant Vercel or Render encrypted environment store. Use separate credentials, databases, Redis instances, Cloudinary folders, Razorpay modes, webhook secrets, and signing secrets in every hosted environment.

`NODE_ENV=production` is correct for both staging and production builds; staging isolation comes from separate services and credentials, not a non-standard Node mode.

## Web application

| Variable                               | Local                     | Test            | Staging                                   | Production                      | Exposure and owner                                       |
| -------------------------------------- | ------------------------- | --------------- | ----------------------------------------- | ------------------------------- | -------------------------------------------------------- |
| `NEXT_PUBLIC_API_URL`                  | `http://127.0.0.1:4000`   | test API origin | staging API HTTPS origin                  | production API HTTPS origin     | Public, Vercel build time                                |
| `NEXT_PUBLIC_SOCKET_URL`               | API origin                | test API origin | staging API HTTPS origin                  | production API HTTPS origin     | Public; Socket.IO upgrades HTTPS to secure WSS           |
| `NEXT_PUBLIC_SITE_URL`                 | `http://127.0.0.1:3000`   | test web origin | Vercel staging/preview HTTPS origin       | canonical HTTPS domain          | Public                                                   |
| `NEXT_PUBLIC_APP_ENV`                  | `local`                   | `test`          | `staging`                                 | `production`                    | Public environment label shown in the admin shell        |
| `NEXT_PUBLIC_GOOGLE_AUTH_ENABLED`      | `false` unless configured | `false`         | approved setting                          | approved setting                | Public feature switch; server credentials remain private |
| `NEXT_PUBLIC_PHONE_AUTH_ENABLED`       | `false` unless configured | `false`         | approved setting                          | approved setting                | Public feature switch                                    |
| `NEXT_PUBLIC_ANALYTICS_PROVIDER`       | unset                     | unset           | approved non-production provider or unset | approved provider or unset      | Public; consent review required                          |
| `NEXT_PUBLIC_ANALYTICS_MEASUREMENT_ID` | unset                     | unset           | staging property ID or unset              | production property ID or unset | Public identifier, not a secret                          |

All `NEXT_PUBLIC_*` values are embedded in browser bundles. Never place a secret in one.

## API and worker runtime

| Variable/group                                                                                      | Local                                                | Test                                  | Staging                                       | Production                                           |
| --------------------------------------------------------------------------------------------------- | ---------------------------------------------------- | ------------------------------------- | --------------------------------------------- | ---------------------------------------------------- |
| `NODE_ENV`                                                                                          | `development`                                        | `test`                                | `production`                                  | `production`                                         |
| `HOST`, `PORT`                                                                                      | local bind and `4000`                                | test bind and port                    | `0.0.0.0`, platform `PORT`                    | `0.0.0.0`, platform `PORT`                           |
| `WEB_ORIGIN`                                                                                        | local web origin                                     | test web origin                       | exact staging HTTPS origin                    | exact canonical HTTPS origin                         |
| `CORS_ORIGINS`                                                                                      | local web origin                                     | test web origin                       | explicit staging/approved preview origins     | canonical origin only                                |
| `TRUST_PROXY_HOPS`                                                                                  | `0`                                                  | `0`                                   | `1` on Render API                             | `1` on Render API                                    |
| `LOG_LEVEL`                                                                                         | `debug` or `info`                                    | `warn`                                | `info`                                        | `info`                                               |
| `MONGODB_URI`                                                                                       | local replica-set URI                                | isolated test database                | staging Atlas SRV secret                      | production Atlas SRV secret                          |
| `REDIS_URL`                                                                                         | local `redis://`                                     | isolated test Redis                   | staging authenticated `rediss://`             | production authenticated `rediss://`                 |
| `SOCKET_REDIS_ADAPTER_ENABLED`                                                                      | `false` is acceptable for one API process            | `false`                               | `true`                                        | `true`                                               |
| `EMAIL_PROVIDER`                                                                                    | `local`                                              | `local`                               | `smtp`                                        | `smtp`                                               |
| `SMTP_HOST`, `SMTP_PORT`, `SMTP_SECURE`, `SMTP_USER`, `SMTP_PASSWORD`, `SMTP_FROM`                  | unset for local provider                             | unset                                 | isolated test sender; encrypted transport     | authenticated production sender; encrypted transport |
| `PAYMENT_PROVIDER`, `RAZORPAY_MODE`                                                                 | `mock`                                               | `mock`                                | `razorpay`, `test`                            | `razorpay`, `live`                                   |
| `RAZORPAY_TEST_KEY_ID`, `RAZORPAY_TEST_KEY_SECRET`, `RAZORPAY_TEST_WEBHOOK_SECRET`                  | optional Test Mode                                   | test fixture/provider mock            | required Razorpay Test Mode secrets           | unset                                                |
| `RAZORPAY_LIVE_KEY_ID`, `RAZORPAY_LIVE_KEY_SECRET`, `RAZORPAY_LIVE_WEBHOOK_SECRET`                  | unset                                                | unset                                 | unset                                         | required live secrets                                |
| `ACCESS_TOKEN_SECRET`, `ACCESS_TOKEN_ISSUER`, `ACCESS_TOKEN_AUDIENCE`                               | local-only secret and identifiers                    | ephemeral test secret and identifiers | unique staging secret and identifiers         | unique production secret and identifiers             |
| `COOKIE_DOMAIN`                                                                                     | unset                                                | unset                                 | staging parent domain only if required        | canonical parent domain only if required             |
| `PRODUCT_PREVIEW_SECRET`                                                                            | local-only secret                                    | ephemeral test secret                 | unique staging secret                         | unique production secret                             |
| `CLOUDINARY_CLOUD_NAME`, `CLOUDINARY_API_KEY`, `CLOUDINARY_API_SECRET`, `CLOUDINARY_PRODUCT_FOLDER` | development account/folder or unset                  | unset/fake adapter                    | staging account/folder                        | production account/folder                            |
| `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `GOOGLE_REDIRECT_URI`                                   | all unset or all configured                          | unset                                 | separate staging OAuth client                 | approved production OAuth client                     |
| `PHONE_AUTH_ENABLED`                                                                                | `false` unless local adapter is intentionally tested | `false`                               | `false` until approved provider is configured | approved setting only                                |
| `TOTP_ENABLED`                                                                                      | `false` unless tested                                | `false`                               | approved setting                              | approved setting                                     |
| `MAX_CART_QUANTITY`                                                                                 | reviewed setting or default                          | test value                            | client-reviewed value                         | client-reviewed value                                |
| `DELIVERY_POSTAL_PREFIXES`                                                                          | optional                                             | test value                            | client-confirmed staging set                  | client-confirmed production set                      |
| `PII_RETENTION_DAYS`                                                                                | policy draft                                         | test value                            | approved policy value                         | approved policy value                                |
| `ALERT_WEBHOOK_URL`                                                                                 | unset                                                | unset                                 | staging alert endpoint                        | production alert endpoint                            |

Admin bootstrap and demo-seed variables are one-time operator inputs, not steady-state service variables:

- `ADMIN_BOOTSTRAP_NAME`, `ADMIN_BOOTSTRAP_EMAIL`, `ADMIN_BOOTSTRAP_PASSWORD`, and `ADMIN_BOOTSTRAP_CONFIRM` are injected only while running the approved bootstrap command, then removed and rotated as applicable.
- `DEMO_SEED_CONFIRM` must never be configured on production services.

The API and worker currently share the validated runtime configuration. Configure the same resource endpoints and provider credentials on both Render services. Only the API receives public traffic; the worker exposes no port.
